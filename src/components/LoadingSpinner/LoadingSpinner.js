import React from 'react';
import PropTypes from 'prop-types';

const LoadingSpinner = props => {
    return (
        <div>
            LoadingSpinner needs to be done ...
        </div>
    );
};

LoadingSpinner.propTypes = {

};

export default LoadingSpinner;