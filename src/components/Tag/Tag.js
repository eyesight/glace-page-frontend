import React from 'react';
import PropTypes from 'prop-types';
import './Tag.scss';

const Tag = props => {
    return (
        <div class="tags"></div>
    );
};

Tag.propTypes = {

};

export default Tag;