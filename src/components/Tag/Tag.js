import React from 'react';
import PropTypes from 'prop-types';
import './Tag.scss';

const Tag = props => {
    return (
        <div className="tags">
            <p className="tags__item">text</p>
        </div>
    );
};

Tag.propTypes = {

};

export default Tag;